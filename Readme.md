# ImageSquoosher

<!-- メインスクリーンショット -->
<img width="49.5%" alt="ImageSquoosher メイン画面（ライトモード）" src="https://github.com/user-attachments/assets/b8e29d08-efd1-468f-a3f4-09690e138785" />
<img width="49.5%" alt="ImageSquoosher メイン画面（ダークモード）" src="https://github.com/user-attachments/assets/8f830f64-1f3f-41cf-ba00-64e61b933d61" /><br />

> [!TIP]  
> **🌐 English version is available: [Readme_English.md](Readme_English.md)**

**エクスプローラーや Finder で写真や画像を選び、右クリックメニューからスパッと開いて一括で圧縮、リサイズ、中央クロップできるデスクトップアプリです。**  
Google Squoosh でも採用されている MozJPEG 4.1.1 の `cjpeg` と、高品質な Lanczos3 リサイズを搭載し、画質を保ったままファイルサイズを極限まで削ぎ落とした JPEG を生成します。Windows 11 と macOS に対応しています。

> [!IMPORTANT]  
> **このアプリは、「Web やブログ、ドキュメントに掲載する画像を、エクスプローラーや Finder の右クリックから最短の手数で最軽量な JPEG に仕上げる」ために作られました。**  
> ブラウザでオンライン圧縮ツールを開いて1枚ずつアップロードしたり、Windows 11 で［その他のオプションを表示］を押して昔ながらのツールを探したりする手間はもう必要ありません。

## Background

Web サイトやブログ記事、技術ドキュメント、SNS などに画像を掲載するとき、スマートフォンやデジタル一眼カメラで撮影した写真や、PC で撮影した高解像度スクリーンショットは、そのままではファイルサイズが大きすぎます。  
数 MB から十数 MB もある画像をそのまま掲載すると、Web ページの表示速度が著しく低下し、閲覧者の通信量も無駄に消費してしまいます。

画像を軽量化する手段として、Google が開発した Web アプリである [Squoosh](https://squoosh.app/) は広く知られており、圧縮品質も優れています。  
私自身も長年重宝していましたが、日常的に記事を書いたりドキュメントを整備していると、**「ブラウザを開いて、画像を1枚ずつドラッグ＆ドロップし、パラメータを確認してダウンロードする」という往復の操作が、何枚も重なると大変なストレスになってきました。**

かといって、デスクトップ向けの既存の一括変換ツールを探してみても、次のような不満が残りました。

- **圧縮エンジンが一般的な libjpeg 止まりで、ファイルサイズが十分に小さくならない**：多くのツールは標準の libjpeg や OS 標準のエンコーダーを使っており、MozJPEG のような高度なトレリス量子化が効かないため、画質を保ったままサイズを削り込めない。
- **リサイズ品質が甘く、縮小すると細部がボケたりモアレが出たりする**：一般的なバイリニア補間などでは、高解像度のスクリーンショットや細い線の入った写真が眠い絵になってしまう。
- **Windows 11 の新しい右クリックメニューに対応していない**：Windows 11 でファイルを右クリックしても、従来のツールは［その他のオプションを表示］(Shift + F10) の中に隠れてしまい、ワンアクションで起動できない。
- **アスペクト比の調整ができない**：ブログのアイキャッチ (16:9) や OGP (1.91:1) に合わせるため、別の画像編集ソフトで事前にトリミングする二度手間が発生する。

「ファイルを選んで右クリックしたら、いつものエクスプローラーや Finder から一瞬で起動して、モダンな UI で確認し、ボタン1つで最高品質の軽量 JPEG に仕上がるツールが欲しい。」  
そう思い立って個人で開発したのが、この **ImageSquoosher** です。

## Motivation

ImageSquoosher は、日常的に画像を扱うクリエイターやエンジニア、ブロガーの作業フローを徹底的に短縮することを目指して設計されています。

### やりたかったこと

- **エクスプローラーや Finder から右クリック一発で開けること**：  
  画像を複数選択して右クリックするだけで、即座に ImageSquoosher が立ち上がり、選択した画像がキューに並びます。Windows 11 では［その他のオプションを表示］の中ではなく、**最初の右クリックメニューに直接表示されます**。macOS でも Finder Sync 拡張により、Finder のコンテキストメニューから直接開けます。
- **MozJPEG による圧倒的な圧縮率と画質の両立**：  
  Google の Squoosh でも使われている MozJPEG 4.1.1 の `cjpeg` バイナリをアプリに同梱。一般的な JPEG エンコーダーと比べて、同じ視覚品質を保ちながらファイルサイズを劇的に削減します。
- **Lanczos3 リサイズと中央クロップの一括処理**：  
  高品質な Lanczos3 フィルタによる縮小処理を搭載。さらに 16:9、4:3、1:1、OGP 向けの 1.91:1 など、よく使う縦横比への「中央自動クロップ」をリサイズと同時に行えます。
- **大切な元画像を絶対に失わせないデータ安全性**：  
  元画像の上書き機能は起動ごとに必ずオフにリセットされます。上書きする場合でも、別の一時ファイルへの出力、MozJPEG の正常終了検証、JPEG 画像としての妥当性検査、作成日時と更新日時の引き継ぎがすべて成功して初めてアトミックに置き換えます。途中でエラーが起きた場合でも元画像は絶対に壊れません。
- **完全ローカル完結、外部通信なしの安心感**：  
  オンラインサービスと異なり、画像データが外部サーバーへ送信されることは一切ありません。機密文書のキャプチャやプライベートな写真も安心して高速に処理できます。

## Design Philosophy

**ImageSquoosher は、「なるべく安全側に倒す」ことと「最短の手数で迷わず処理できること」を最優先に設計しています。**

### 絶対に守るルール

- **元画像は原則として保持する**：  
  既定では `_resized` のサフィックスを付けた別名で保存します。元画像を誤って消してしまう事故を根本から防ぎます。
- **同名ファイルは暗黙に上書きしない**：  
  出力先に同じ名前のファイルが既に存在する場合は、` (1)`、` (2)` のように Finder やエクスプローラーと同じ形式の連番を自動で付与します。
- **上書きは厳格な検証を通過した時だけ行う**：  
  ［元のファイルを上書きする］を明示的に有効にした場合でも、別の一時パスへ書き出し、MozJPEG の終了コード、出力の JPEG シグネチャ、出力寸法、ファイル日時の引き継ぎがすべて確認できるまで、元画像には一切手を加えません。
- **起動時に危険な設定を残さない**：  
  ［元のファイルを上書きする］のチェックは、アプリを起動するたびに必ずオフへ戻ります。

## Features

### ⚡ エクスプローラーや Finder の右クリックから一瞬で開ける

<!-- スクリーンショット挿入位置: Windows 11 の右クリックメニューと macOS の Finder メニュー -->
<img width="600" alt="Windows 11 の右クリックメニューに直接表示された ImageSquoosher" src="" /><br />

Windows 11 のエクスプローラーでも、macOS の Finder でも、画像ファイルを選択して右クリックするだけで即座に起動し、選択した画像が変換キューに入ります。  
Windows 11 では「その他のオプションを表示」を開く必要がなく、最初の右クリックメニューのトップレベルに直接［ImageSquoosher］が表示されます。  
macOS でも Finder で画像を選択したときのコンテキストメニューに直接組み込まれます。  
複数ファイルを選択して右クリックすれば、選択したすべての画像が一括でキューに追加されます。もちろん、起動中のウィンドウへ画像ファイルをドラッグ＆ドロップすることでも追加できます。

### 🗜️ MozJPEG 4.1.1 搭載による高圧縮と高画質

<!-- スクリーンショット挿入位置: 圧縮前後のファイルサイズ比較 -->
<img width="600" alt="MozJPEG によるファイルサイズ削減結果" src="" /><br />

JPEG 出力には、実績ある MozJPEG 4.1.1 の `cjpeg` を直接使用しています。  
トレリス量子化をはじめとする MozJPEG 独自の最適化処理により、見た目の劣化を感じさせないまま、通常の JPEG 出力よりも大幅にファイルサイズを小さく抑えられます。  
品質スライダー（既定値：90）で、用途に応じたバランスを直感的に指定できます。

### 📐 Lanczos3 リサイズと選べる中央自動クロップ

<!-- スクリーンショット挿入位置: 変換設定パネル -->
<img width="600" alt="変換設定パネル（縦横比、品質、リサイズ、サフィックス）" src="" /><br />

縮小アルゴリズムには、縮小後も文字や細部がボケにくい **Lanczos3 (ランチョスフィルタ)** を採用しています。  
スクリーンショットの文字や細部のディテールも鮮明に保ったままリサイズできます。  
さらに、元画像の縦横比を保つだけでなく、16:9 (YouTube サムネイルやブログ)、1.91:1 (OGP 画像)、1:1 (正方形)、4:3、3:2 など、主要なアスペクト比に中央を基準にして一括クロップできます。拡大（アップスケール）を防ぐ安全ガードも標準で有効です。

### 🛡️ 徹底したデータ安全性と日時の引き継ぎ

<!-- スクリーンショット挿入位置: 元画像上書き設定と警告 -->
<img width="600" alt="元ファイルの上書き設定" src="" /><br />

「誤操作で大切な元画像を消してしまった」という事故を防ぐため、**［元のファイルを上書きする］設定はアプリを起動するたびに必ずオフへ戻ります**。  
また、上書きを有効にした場合でも、別の一時ファイルに出力して検証（MozJPEG の終了コード、JPEG シグネチャ、要求寸法）をパスした後にのみ原子的に置き換えます。  
変換後のファイルには、元画像の作成日時と更新日時を正確に引き継ぎます (macOS は `URLResourceValues`、Windows は Win32 API 経由)。  
PNG や WebP の上書き時も、空いている JPEG 名を確保して生成と検証の成功後に元ファイルを削除するため、既存の同名 JPEG を壊す心配がありません。

### 🎨 洗練された使い心地のモダン UI

<!-- スクリーンショット挿入位置: 画像一覧と進捗表示 -->
<img width="600" alt="画像一覧と進捗表示" src="" /><br />

各画像カードには、入出力ファイル名、変換前後の解像度、元サイズと圧縮後のファイルサイズ、削減率（%）がリアルタイムにわかりやすく表示されます。  
一覧からダブルクリックで元画像を既定のビューアーで開いたり、変換完了後に［Finder で表示］（Windows では［フォルダを開く］）から直接保存先にジャンプできます。

### 🔒 完全ローカル処理とプライバシー保護

画像処理はすべてお使いの PC 内で完結します。  
インターネットへの画像アップロードや外部通信は一切行われないため、機密情報を含むドキュメント画像や個人的な写真も安心して処理できます。

## 対応環境

| OS | アーキテクチャ | 備考 |
| --- | --- | --- |
| Windows 11 | x64 | 最初の右クリックメニューに対応 |
| macOS 12 以降 | Apple Silicon (arm64) | Finder Sync 拡張に対応 |

## 対応画像形式

| 入力形式 | 出力形式 | 処理内容 |
| --- | --- | --- |
| JPEG (`.jpg`, `.jpeg`) | JPEG (`.jpg`) | 静止画のみ対応 |
| PNG (`.png`) | JPEG (`.jpg`) | 透過部分は白背景と自動合成 |
| WebP (`.webp`) | JPEG (`.jpg`) | 静止画のみ対応 |

※ アニメーション PNG やアニメーション WebP は変換対象外です。

## インストールとセットアップ

### ダウンロード

1. [Releases ページ](https://github.com/tsukumijima/ImageSquoosher/releases) を開きます。
2. お使いの OS に対応した ZIP ファイルをダウンロードします。
   - Windows: `ImageSquoosher-vX.Y.Z-windows.zip`
   - macOS: `ImageSquoosher-vX.Y.Z-macos.zip`
3. ダウンロードした ZIP ファイルを展開します。

### Windows での起動と右クリックメニュー登録

<!-- スクリーンショット挿入位置: Windows での展開後フォルダ -->
<img width="600" alt="Windows での展開後フォルダ" src="" /><br />

1. 展開したフォルダを任意の場所へ配置します。  
   アプリ本体と同じ場所にある `mozjpeg` フォルダや VC++ ランタイム DLL も動作に必要ですので、フォルダごと配置してください。
2. `ImageSquoosher.exe` をダブルクリックして起動します。
3. **右上のパズルボタン［Explorer の右クリックメニューに追加］をクリックします。**  
   Windows のユーザーアカウント制御（UAC）画面が表示されるので、承認します。
4. これで登録完了です。エクスプローラーで画像を選択して右クリックすると、**最初のメニューに直接［ImageSquoosher］が表示されます**。

> [!TIP]  
> - **ポータブル版のまま登録可能**：ImageSquoosher はインストーラー不要のポータブルアプリですが、内部で loose package 登録の仕組みを使用しているため、Windows 11 の最新右クリックメニューに直接追加できます。
> - **配置場所を移動した場合**：フォルダを別の場所へ移動した場合は、右上のボタンが［Explorer の右クリック連携を修復］に変わりますので、クリックして修復してください。
> - **連携を解除したい場合**：右上のボタン［Explorer の右クリックメニューから削除］をクリックするだけで、いつでも右クリック項目を綺麗に削除できます。

### macOS での起動と Finder 連携の有効化

<!-- スクリーンショット挿入位置: macOS での Finder Sync 設定画面 -->
<img width="600" alt="macOS Finder Sync 設定画面" src="" /><br />

1. 展開して現れた `ImageSquoosher.app` を「アプリケーション」フォルダへ移動します。
2. `ImageSquoosher.app` を起動します。
3. **右上のパズルの形をした Finder 連携ボタンをクリックします。**  
   macOS の「システム設定」の機能拡張管理画面が開きます。
4. 一覧から **ImageSquoosher Finder Sync** 拡張を有効（オン）にします。
5. これで設定完了です。Finder で画像を選択して右クリックすると、**コンテキストメニューに［ImageSquoosher で圧縮・リサイズ］が表示されます**。

## 基本的な使い方

<!-- スクリーンショット挿入位置: 操作手順のハイライト -->
<img width="600" alt="ImageSquoosher の操作手順" src="" /><br />

1. **画像を追加する**：  
   エクスプローラーや Finder で画像を選んで右クリックメニューから開くか、起動中のウィンドウへ画像ファイルをドラッグ＆ドロップします。ヘッダーの［画像を追加］ボタンからも選択できます。
2. **変換設定を調整する**：  
   左側の設定パネルで、必要に応じて変換パラメータを調整します。
   - **画質**：JPEG の品質を 1から100 で指定します（既定値：90）。
   - **縦横比**：中央クロップの比率を選択します。元画像の比率を保つ「Original」のほか、16:9、1.91:1 (OGP)、1:1 (正方形)、4:3、3:2 などから選べます。
   - **リサイズ**：幅または高さを基準に、指定したピクセル数へリサイズします。
   - **メタデータ削除**：位置情報 (GPS) やカメラ撮影情報 (EXIF) を出力から除外したい場合は有効にします。
   - **サフィックス**：出力ファイル名の末尾に付与する文字列を指定します（既定値：`_resized`）。
3. **変換を開始する**：  
   画面下の［変換開始］ボタンをクリックします。画像が順次変換され、進捗バーと削減率がリアルタイムに更新されます。
4. **出力ファイルを確認する**：  
   変換が完了したら、カード右下の［出力ファイルを開く］で直接ファイルを表示するか、［Finder で表示］（Windows では［フォルダを開く］）をクリックして保存先フォルダを開きます。

### 出力ファイル名の規則

- 通常は `{元のファイル名}{サフィックス}.jpg` として出力されます。  
  例：`photo.png` → `photo_resized.jpg`
- 出力先に同名のファイルが既に存在する場合は、上書きを避けるため連番を付与します。  
  例：`photo_resized (1).jpg`、`photo_resized (2).jpg`
- 元のファイル名の末尾に連番が含まれている場合は、サフィックスの後ろへ連番を引き継ぎます。  
  例：`photo (2).png` → `photo_resized (2).jpg`

### 元画像の上書きについて

［元のファイルを上書きする］を有効にすると、変換後の JPEG で元画像を置き換えます。

> [!WARNING]  
> 元画像の上書きは不可逆な操作です。必ず事前に代表的な画像で変換結果を確認してからご利用ください。  
> 既定の状態では上書きはオフになっており、アプリを再起動した際も必ずオフに戻ります。

- **JPEG 画像の上書き**：一時ファイルで MozJPEG 変換、JPEG の正当性、寸法の合致を確認した上で、アトミックに置き換えます。万が一変換途中で問題が発生した場合も元画像は保持されます。
- **PNG / WebP 画像の上書き**：空いている JPEG ファイル名を確保して変換と検証を完了させ、作成日時と更新日時を引き継いだ後でのみ元画像を削除します。同名の既存 JPEG を誤って削除することはありません。

## 技術的なこだわり

### なぜ MozJPEG なのか

一般的な画像変換ツールでは、処理の軽さや実装の手軽さから標準の libjpeg や OS 提供のエンコーダーが使われることがほとんどです。  
しかし、これらは視覚的な冗長性を削ぎ落とす最適化が不十分で、ファイルサイズを小さくしようと品質設定を下げると、すぐに目立つブロックノイズや色にじみが発生してしまいます。

MozJPEG は、Google も推進するオープンソースの高度な JPEG エンコーダーです。  
人間の視覚特性に合わせた量子化テーブルの自動調整や、トレリス量子化による徹底的なデータ削減を行うため、**「見た目の美しさを保ったまま、ファイルサイズだけを劇的に落とす」** ことができます。  
ImageSquoosher では、システムの固定パスに依存せず動作するよう、各プラットフォーム向けにビルドした MozJPEG 4.1.1 の `cjpeg` ネイティブバイナリを同梱し、変換コアから直接呼び出しています。

### なぜ Lanczos3 リサイズなのか

画像を縮小する際、単純な最近傍法（Nearest Neighbor）やバイリニア補間（Bilinear）を使用すると、文字の輪郭がぼやけたり、細い格子模様に激しいモアレ（干渉縞）が発生したりします。  
ImageSquoosher では、高品質なサンプリングフィルタである **Lanczos3 (3次ランチョスフィルタ)** を採用しています。  
スクリーンショットに含まれる細かな文字の可読性を保ち、高精細な写真でもくっきりとした解像感を維持したまま縮小できます。

### Windows 11 の最新右クリックメニューへの直接登録

Windows 11 ではコンテキストメニューのデザインが一新され、従来のシェルの拡張機能は［その他のオプションを表示］の配下に押し込められてしまいました。  
新しいトップレベルのメニューに項目を追加するには、通常 MSIX パッケージでの配布や自己署名証明書のインストールが要求されます。

ImageSquoosher は、インストーラー不要で手軽に使えるポータブル ZIP 配布の利便性を維持しながら、Windows 11 の最新メニューに直接表示させることにこだわりました。  
同梱の登録ヘルパー（`ImageSquoosherShellRegistration.exe`）が、展開されたフォルダ内の `AppxManifest.xml` を loose package として現在のユーザー環境へ登録します。登録時に必要な開発者モードの変更のみを UAC 付きヘルパーで一時的に行い、登録完了後は即座に変更前の状態へ復元します。  
これにより、ポータブルアプリでありながら Windows 11 ネイティブの快適な操作感を実現しています。

## 開発

### 必要な環境

- Flutter 3.35.7 と同梱の Dart 3.9.2
- macOS: Xcode、CMake
- Windows: Visual Studio 2022 (「C++ によるデスクトップ開発」)、PowerShell、CMake

開発規範の詳細については [AGENTS.md](AGENTS.md) を参照してください。

### Debug の開発手順

画面開発には Debug アプリとホットリロードを使用します。

macOS:

```bash
flutter pub get
tools/build_mozjpeg.sh
flutter run -d macos
```

Windows:

```powershell
flutter pub get
pwsh -File tools/build_mozjpeg.ps1
flutter run -d windows
```

### テストと静的解析

コードの編集後は、リンター、フォーマッター、ユニットテストを実行します。

```bash
dart format --output=none --set-exit-if-changed .
flutter analyze
flutter test
```

実バイナリの MozJPEG を指定した変換テスト：

macOS:

```bash
IMAGE_SQUOOSHER_CJPEG="$PWD/native/mozjpeg/macos/arm64/cjpeg" \
  flutter test test/services/image_conversion_pipeline_test.dart
```

Windows:

```powershell
$env:IMAGE_SQUOOSHER_CJPEG = "$PWD\native\mozjpeg\windows\cjpeg.exe"
flutter test test/services/image_conversion_pipeline_test.dart
```

### リリースビルド

macOS (Apple Silicon):

```bash
tools/build_mozjpeg.sh
flutter build macos --release
tools/bundle_mozjpeg.sh build/macos/Build/Products/Release/ImageSquoosher.app
```

Windows (x64):

```powershell
pwsh -File tools/build_mozjpeg.ps1
flutter build windows --release
pwsh -File tools/bundle_mozjpeg.ps1
```

## License

[MIT License](License.txt)
